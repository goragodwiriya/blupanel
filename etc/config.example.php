<?php

declare (strict_types = 1);

/**
 * ค่าตั้งของ PHP Server Control Panel
 *
 * ติดตั้งแบบ system : คัดลอกไปที่ /etc/phpcp/config.php (สิทธิ์ 0640 root:phpcp)
 * ทดสอบแบบ portable : คัดลอกเป็น etc/config.php ในโฟลเดอร์โปรเจกต์
 *
 * ไฟล์นี้มี secret key จึงห้าม world-readable และห้าม commit เข้า git
 */

return [
    /**
     * production — ทำงานกับระบบจริง ต้องรัน agent ด้วย root
     * sandbox    — เปลี่ยนเส้นทางทุกอย่างเข้า prefix ไม่แตะระบบจริง (สำหรับเครื่องทดสอบ)
     * dryrun     — ไม่ทำอะไรเลย แสดงเฉพาะคำสั่งที่จะรัน
     */
    'mode' => 'sandbox',

    /**
     * system   — /etc/phpcp, /var/lib/phpcp, /run/phpcp, /var/log/phpcp
     * portable — ทุกอย่างอยู่ใต้โฟลเดอร์โปรเจกต์ (ใช้ตอนพัฒนา ไม่ต้องใช้ root)
     * ว่าง     — ตรวจจากสภาพเครื่องให้อัตโนมัติ
     */
    'layout' => 'portable',

    'panel' => [
        'port' => 8443,
        'base_url' => '',

        // ต้องเป็น true เมื่อให้บริการผ่าน HTTPS จริง (คุกกี้จะใช้ prefix __Host- ให้เอง)
        'cookie_secure' => false,

        'session_ttl' => 28800,
        'session_idle' => 1800,
        'session_rotate' => 900,

        // จำกัด IP ที่เข้า panel ได้ เช่น ['203.0.113.0/24', '10.0.0.5'] — ว่าง = ไม่จำกัด
        'ip_allowlist' => [],
        'trusted_proxies' => []
    ],

    'agent' => [
        'socket' => '',
        'timeout' => 30,

        // uid ที่ยอมให้ต่อ socket ได้ ตรวจด้วย SO_PEERCRED **เสมอ** ไม่ใช่เฉพาะเมื่อตั้งค่าไว้
        //
        // root กับ uid ของ agent เองอยู่ในรายการโดยปริยาย · ปกติไม่ต้องใส่อะไรที่นี่เลย
        // ใส่เมื่อชั้นเว็บรันด้วยผู้ใช้ที่ไม่ใช่ `allowed_users` ข้างล่าง
        'allowed_uids' => [],

        // ชื่อผู้ใช้ของชั้นเว็บ — แปลงเป็น uid ตอนตรวจ ไม่ใช่ตอนติดตั้ง เพราะ uid ของ
        // บัญชีระบบต่างกันไปในแต่ละเครื่อง เลขที่ฝังไว้จะกลายเป็นของผู้ใช้คนอื่นเมื่อย้ายเครื่อง
        'allowed_users' => ['phpcp-web']
    ],

    'sandbox' => [
        'prefix' => ''
    ],

    'sites' => [
        // ที่เก็บไฟล์ของเว็บไซต์ทุกเว็บ — โครงสร้างคือ <dir>/<โดเมน>/{public,logs,tmp,backups}
        // เปลี่ยนค่านี้แล้วเว็บเดิมจะหาไฟล์ไม่เจอ ต้องย้ายโฟลเดอร์ตามแล้วรัน `phpcp sites:rebuild`
        //
        // **ใช้กับเลย์เอาต์เก่าเท่านั้น** — ตั้งแต่ migration 0006 ไฟล์เว็บอยู่ใต้บ้านของ
        // เจ้าของ (`users_dir` ข้างล่าง) ค่านี้เหลือไว้เพื่ออ่านของเดิมที่ยังไม่ได้ย้าย
        'dir' => '/srv/phpcp/sites',

        /**
         * บ้านของผู้ใช้โฮสติ้ง — ไฟล์เว็บอยู่ที่ `<users_dir>/<ผู้ใช้>/public_html`
         *
         * ค่าเริ่มต้นคือ `/home` ซึ่งเป็นที่ที่ผู้ดูแลระบบยูนิกซ์ทุกคนคาดว่าจะเจอบ้านผู้ใช้
         * และเป็นที่เดียวกับที่ cPanel/DirectAdmin/Plesk ใช้ · ลูกค้าที่ย้ายมาจึงเจอ
         * เส้นทางเดิมที่คุ้นเคย และสคริปต์ deploy ของเขาไม่ต้องแก้
         *
         * เครื่องพัฒนาที่เก็บโปรเจกต์ไว้ที่อื่นตั้งเป็นที่นั้นได้ เช่น '/mnt/Server/htdocs'
         *
         * เปลี่ยนค่านี้บนเครื่องที่มีเว็บอยู่แล้ว **ต้องย้ายโฟลเดอร์ตามด้วยมือ** แล้วรัน
         * `phpcp sites:rebuild` — ระบบไม่ย้ายให้เพราะไม่รู้ว่าปลายทางมีอะไรอยู่ก่อน
         */
        'users_dir' => '/home',

        /**
         * รูปทรงไฟล์ใต้บ้านผู้ใช้ — 'cpanel' (ค่าเริ่มต้น) หรือ 'phpcp' (ของเดิม)
         *
         *   cpanel  <บ้าน>/public_html          โดเมนหลักของบัญชี
         *           <บ้าน>/<โดเมน>              โดเมนที่เพิ่มทีหลัง
         *   phpcp   <บ้าน>/domains/<โดเมน>/public
         *
         * ตั้งรายบัญชีทับค่านี้ได้จากหน้าผู้ใช้ · ดู Phpcp\Domain\SiteLayout
         */
        'layout' => 'cpanel',

        /**
         * ยอมรับว่า filesystem ที่เก็บเว็บไซต์เก็บเจ้าของไฟล์ไม่ได้ จึงข้ามการแยกสิทธิ์ระหว่างเว็บ
         *
         * เปิดได้เฉพาะเมื่อ dir อยู่บน NTFS / exFAT / FAT เท่านั้น — agent จะทดสอบ chown จริง
         * ก่อนทำงานทุกครั้ง ถ้า filesystem รองรับ ownership อยู่แล้วจะปฏิเสธจนกว่าจะปิดค่านี้
         * ตั้งใจให้เป็นแบบนี้ เพื่อไม่ให้ค่านี้หลุดขึ้น server จริงแล้วลดความปลอดภัยโดยเงียบ ๆ
         *
         * เปิดแล้วจะเสียอะไร: เว็บหนึ่งอ่าน/เขียนไฟล์ของอีกเว็บได้ในระดับ filesystem
         * สิ่งที่ยังกันอยู่: open_basedir และ disable_functions ของแต่ละ FPM pool
         *
         * ห้ามใช้กับเครื่องที่ให้บริการเว็บของคนอื่น
         */
        'shared_owner' => false,

        /**
         * โฟลเดอร์นอก dir ที่ยอมให้ชี้ DocumentRoot เข้าไปได้ (Domain Pointer)
         *
         * ว่าง = ชี้ได้เฉพาะภายใน dir เท่านั้น
         * ใส่เฉพาะโฟลเดอร์ที่เก็บโค้ดเว็บจริง ๆ ห้ามใส่ / หรือ /etc หรือ /home
         * เพราะเท่ากับเปิดให้สร้าง vhost เสิร์ฟไฟล์ทั้งเครื่องผ่านเว็บ
         */
        'pointer_roots' => [],

        /**
         * โฟลเดอร์ที่เสิร์ฟที่ http://localhost — สำหรับเครื่องพัฒนาเท่านั้น
         *
         * **ว่าง = ปิด** และเครื่องที่ให้บริการจริงต้องปล่อยว่างไว้ · เปิดเมื่อไหร่
         * แปลว่าโค้ดทุกอย่างใต้โฟลเดอร์นี้เปิดผ่านเว็บได้ ซึ่งบนเครื่องพัฒนามักเป็น
         * ที่เก็บงานทุกโปรเจกต์รวมกัน
         *
         * มีไว้เพื่อไม่ต้องเขียนไฟล์ vhost เอง — panel สร้างให้ตามโหมดที่ใช้อยู่
         * ทุกครั้งที่รัน `phpcp sites:rebuild` (ซึ่งตัวติดตั้งเรียกให้เองตอนจบ และ
         * การสลับโหมดก็เรียก) · vhost ที่เขียนมือไว้จะหายไปเงียบ ๆ ตอนสลับโหมด
         * เพราะไม่มีใครแปลงให้ — อันนี้ไม่หาย
         *
         * ผูกกับ 127.0.0.1 เท่านั้น เครื่องอื่นในวงเน็ตเรียกไม่ได้
         */
        'localhost_docroot' => '',

        /**
         * เวอร์ชัน PHP ที่ localhost ใช้ — ว่าง = เวอร์ชันเดียวกับที่ panel รันอยู่
         *
         * ใช้ FPM pool มาตรฐานของดิสโทร (`/run/php/php<ver>-fpm.sock` ซึ่งรันเป็น
         * www-data) ไม่ใช่ pool ของบัญชีลูกค้า — โฟลเดอร์พัฒนาไม่ใช่ของลูกค้าคนไหน
         * และไม่ควรถูก chown หรือถูกจำกัดด้วย open_basedir ของบัญชีใคร
         */
        'localhost_php' => ''
    ],

    // เว็บเซิร์ฟเวอร์ที่ใช้โฮสต์เว็บไซต์ของผู้ใช้
    //
    //   'apache'      Apache ตัวเดียวรับตั้งแต่พอร์ต 80/443 — .htaccess ใช้ได้เต็ม
    //   'nginx'       nginx ตัวเดียว — เร็วที่สุด แต่ **ไม่มี .htaccess เลย**
    //                 เว็บที่พึ่งไฟล์นั้นต้องแปลงกฎเป็นรูปแบบของ nginx เอง
    //   'nginx-proxy' nginx รับหน้า (จบ TLS, กันผู้ใช้เน็ตช้า) แล้วส่งต่อให้ Apache
    //                 ที่ 127.0.0.1:8080 ซึ่งอ่าน .htaccess ให้ตามปกติ
    //                 — เลือกตัวนี้ถ้าย้ายเว็บมาจากโฮสต์อื่นและต้องการ .htaccess
    //
    // เปลี่ยนค่านี้แล้วต้องสร้างไฟล์ vhost ของทุกเว็บไซต์ใหม่ทั้งหมด
    // (`phpcp sites:rebuild`) เพราะไฟล์เดิมเป็นรูปแบบของเซิร์ฟเวอร์ตัวเก่า
    //
    // โหมด nginx-proxy เขียนทับ /etc/apache2/ports.conf ให้ Apache ฟังเฉพาะ loopback
    // และ **ต้องรีสตาร์ต Apache หนึ่งครั้ง** ไม่ใช่แค่ reload เพราะการเปลี่ยนพอร์ตที่ฟัง
    // ทำตอน reload ไม่ได้ตามการออกแบบของ Apache เอง
    // ค่าเริ่มต้นคือโหมดที่ใช้ได้กับทุกเว็บ — .htaccess ทำงานเหมือนโฮสต์ทั่วไป
    // และได้ nginx รับหน้าไปด้วย · เปลี่ยนได้จากหน้า Settings ไม่ต้องแก้ไฟล์นี้
    'webserver' => 'nginx-proxy',

    'security' => [
        // สร้างด้วย `bin/phpcp key:generate` — เป็น base64 ของข้อมูล 32 ไบต์
        'secret_key' => '',

        'require_2fa_roles' => ['superadmin', 'sysadmin'],
        'max_login_attempts' => 5,
        'lockout_seconds' => 900,
        'password_min_length' => 12
    ],

    'log' => [
        'level' => 'info'
    ]
];
