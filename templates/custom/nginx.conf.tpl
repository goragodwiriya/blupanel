# ค่าตั้งเพิ่มเติมของ {{DOMAIN}} — ไฟล์นี้เป็นของผู้ดูแลเครื่อง
#
# panel ไม่เขียนทับไฟล์นี้เลย · แก้จากหน้าเว็บไซต์ใน panel หรือแก้ไฟล์นี้ตรง ๆ ก็ได้
#
# สามข้อที่ควรรู้ก่อนเขียน
# -----------------------------------------------------------------------------
# 1. ไฟล์นี้ถูกอ่าน **ท้ายสุด** หลังค่าตั้งที่ panel สร้าง — เขียนเฉพาะสิ่งที่ต้องการ
#    เปลี่ยนพอ ไม่ต้องเขียนใหม่ทั้งชุด
#    ข้อควรระวังของ nginx: `location` ที่ซ้ำกับของเดิมจะชนกัน ไม่ใช่เขียนทับ
#    ให้ใช้เส้นทางที่ยังไม่มี หรือใช้คำสั่งระดับ server อย่าง add_header แทน
#
# 2. อยู่ในบริบท server { } — คำสั่งระดับเครื่อง (worker_processes, events, http)
#    ใช้ที่นี่ไม่ได้ nginx จะปฏิเสธตอนตรวจค่าตั้ง ซึ่งเป็นด่านที่ทำงานก่อนบันทึกจริง
#
# 3. บันทึกแล้วต้องเปิดเว็บดูว่าใช้งานได้จริงแล้วกดยืนยัน — การตรวจค่าตั้งพิสูจน์แค่ว่า
#    ไวยากรณ์ถูก กฎที่บล็อกทุกคำขอก็ผ่านการตรวจเหมือนกัน · ไม่กดยืนยันแล้วระบบคืนค่าเดิม
#
# ดูค่าตั้งที่ panel สร้างให้ได้จากปุ่มเปิดไฟล์ในตารางเดียวกัน
# -----------------------------------------------------------------------------

# ตัวอย่าง — ลบเครื่องหมาย # หน้าบรรทัดที่ต้องการใช้

# ส่วนหัวความปลอดภัยที่ panel ไม่ได้ตั้งให้
#add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#add_header X-Frame-Options "SAMEORIGIN" always;
#add_header Referrer-Policy "strict-origin-when-cross-origin" always;

# ตั้งอายุแคชของไฟล์ static
#location ~* \.(jpg|jpeg|png|gif|webp|svg|css|js|woff2)$ {
#    expires 30d;
#    add_header Cache-Control "public";
#}

# ขนาดไฟล์อัปโหลดสูงสุด (panel ตั้งค่าเริ่มต้นให้แล้ว เขียนที่นี่เพื่อทับ)
#client_max_body_size 128m;
